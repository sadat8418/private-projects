pip install virtualenv

pip install --upgrade setuptools

pip install wheel

virtualenv AR


which python3

alias python='/usr/bin/python3'


[[[[ virtualenv -p python3.7.3 Sadat 
    virtualenv --python=/usr/bin/python3.6 python36

]]]]

pip install pygame==2.0.0.dev6


virtualenv -p /usr/local/bin/python3 venv38

apt show python-pip
-----------------------------
nano ~/.bashrc 

aliasssssssssssssss 
	alias pip2='/usr/bin/pip3'
	#alias pip='/usr/bin/pip3'
	alias python2='/usr/bin/python3'

 
source ~/.bashrc
----------------------------
Homebrew:

Install up-to-date versions of software when your host distribution is old

git clone https://github.com/Homebrew/brew ~/.linuxbrew/Homebrew
mkdir ~/.linuxbrew/bin
ln -s ~/.linuxbrew/Homebrew/bin/brew ~/.linuxbrew/bin
eval $(~/.linuxbrew/bin/brew shellenv)
--------------------------------

pip freeze > requirements.txt
virtualenv --system-site-packages Sadat


pip4 & python4 ====> virtualenv 
pip2 & python2 ====> default
-------------------------------------
